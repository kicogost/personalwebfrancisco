PLACEHOLDER. Francisco writes this. Everything below is scaffolding to show how
the page sets, and should be deleted rather than edited around.

Plain prose. A blank line between paragraphs, and nothing else needed. The page
sets it in the body face at a 62 character measure, which is about as wide as a
line can get before the eye starts losing its place on the return sweep.

Markdown headings work if the argument needs them. The page reads better
without, so the default assumption is that it runs as continuous prose and the
paragraph breaks carry the structure on their own.

Long enough to be worth the click, short enough to finish standing up.
